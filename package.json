{
  "name": "sistema-provas",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de questões de provas",
  "private": true,
  "scripts": {
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "install:features": "cd features && npm install",
    "install:all": "npm run install:server && npm run install:client && npm run install:features",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "test": "cd features && npm test",
    "build": "cd server && npm run build && cd ../client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
